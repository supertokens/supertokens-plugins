{
  "name": "@supertokens-plugins/rownd-python",
  "version": "0.1.0",
  "private": true,
  "description": "Rownd migration plugin for SuperTokens Python",
  "scripts": {
    "sync": "uv sync --dev",
    "build": "uv run python -m build",
    "test": "uv run pytest",
    "lint": "uv run ruff check . && uv run pyright",
    "clean": "rm -rf .turbo dist build src/*.egg-info .pytest_cache .ruff_cache src/supertokens_rownd/__pycache__ tests/__pycache__"
  },
  "keywords": [
    "supertokens",
    "rownd",
    "migration",
    "plugin",
    "python"
  ],
  "license": "Apache-2.0"
}
